{
  "name": "servidor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}

